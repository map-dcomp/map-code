sim_output - this is the output from the simulation in lo-fi and the directory inside the tar file from hi-fi

scenario dir - for lo-fi use the input scenario directory, for hi-fi use the output directory from stage experiment (this ensures that IP addresses are found)

demand dir - for lo-fi use the input demand directory, for hi-fi use client/demand inside the output from stage experiment
